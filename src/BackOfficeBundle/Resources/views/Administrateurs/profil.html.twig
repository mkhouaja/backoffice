{% extends "BackOfficeBundle:Pages:base.html.twig" %}
{% block content %}
    <div id="wrapper">
        <nav>
            {% include 'BackOfficeBundle:Pages:bonjour.html.twig' %} 
            {% include 'BackOfficeBundle:Pages:navigation.html.twig' %}
            {% include 'BackOfficeBundle:Pages:navbar.html.twig' %} 
        </nav>
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default margin">
                        <div class="panel-heading">
                            Modifier mon profil
                        </div>
                        <div class="panel-body">
							{{ form_start(edit_form) }}
                             <fieldset>
							   <legend>Mon profil:</legend>
                               {{ form_widget(edit_form) }}
                                <input type="submit" class="btn btn-warning btn-sm" value="Enregistrer les modifications" />
                             </fieldset>
                            {{ form_end(edit_form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}